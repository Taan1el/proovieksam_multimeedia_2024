<!doctype html>
<html lang="et">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || "NEXUS – Moodul 4" %></title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="nav">
      <div class="container nav-inner">
        <a class="brand" href="/shop">
          <div class="logo"></div>
          <span class="brand-title">NEXUS</span>
        </a>
        <div class="nav-links">
          <a class="pill" href="/shop">E-pood</a>
          <a class="pill" href="/contact">Kontakt</a>
          <% if (user) { %>
            <a class="pill" href="/admin/products">Admin</a>
            <form method="POST" action="/admin/logout" style="display:inline">
              <button class="pill" type="submit">Logi välja</button>
            </form>
          <% } else { %>
            <a class="pill" href="/admin/login">Admin</a>
          <% } %>
        </div>
      </div>
    </div>
    <div class="container">

